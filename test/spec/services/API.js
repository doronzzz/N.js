'use strict';

describe('Service: API', function () {
    describe('method: getToken', function(){
        var realUrl = 'token.url';
        it('should make a call to real url when called', function () {
            var url = realUrl;
/*            url.get(function(){
            	expect(req.data.dummy).toBe(1);
            });
*/
            expect(1).toBe(1); 
        });
    });
});
